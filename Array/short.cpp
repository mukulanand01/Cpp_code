// move all postive number at begining and and all negative number at ending witj constant extra space 
#include<iostream>
#include<vector>
using namespace std;
void shot(vector<int>& v ){
  for(int i=0,j=v.size();i<v.size();i++,j--){
  if (
       
  )
  }
  
}

int main(){
  vector<int>v;
  v.push_back(1);
  v.push_back(-2);
  v.push_back(3);
  v.push_back(-4);
  v.push_back(-5);
  v.push_back(6);
  shot(v);
}